# niuma-engine — AI 协作工程框架

> 本文件是 AI 代理的根指南。代理启动时自动加载。

---

## 核心信条（不可违反）

1. **诚实决定价值** — 口径不实的高分是负资产
2. **效率来自并行与不重复** — 死路必须归档
3. **边界自觉** — 成熟的交付 = 准确知道做到了哪

---

## 启动序列（每次对话执行）

| Step | 操作 | 说明 |
|------|------|------|
| 1 | Read `memory/MEMORY.md` | 读取长期记忆 |
| 2 | Read `memory/{今天日期}.md` | 读取当日记忆（如存在） |
| 3 | 检查是否有待处理的记忆候选 | 上次对话遗留的确认项 |
| 4 | 开始处理用户消息 | 完成以上步骤后才能开始 |

---

## 规则加载

`.claude/rules/` 目录下的所有规则文件由 Claude Code 自动加载。其他代理请参考 `docs/agent-install.md`。

**规则优先级**：用户指令 > 项目 CLAUDE.md > `.claude/rules/` > 代理默认行为

---

## 记忆管理

### 三层架构

```
全局规则（始终生效）
  ├── .claude/rules/*.md
  └── CLAUDE.md
项目记忆（按项目加载）
  └── memory/MEMORY.md（长期记忆主索引）
个人记忆（按需加载）
  └── memory/{YYYY-MM-DD}.md（每日记忆）
```

### 写入规则

- **禁止未经确认写入**：任何记忆写入前必须经过用户确认（记忆候选机制）
- **Write 后必须 Read**：任何文件写入后必须读取验证
- **先 Read 再 Write**：修改任何文件前必须先读取当前内容
- **禁止盲目覆写**：不了解当前内容就写入 = 数据事故

### 置信度标签

每条记忆必须标注置信度：
- `[高置信]` — 用户明确确认 ≥ 2 次
- `[中置信]` — AI 观察到的规律
- `[待确认]` — 单次观察，需用户确认

---

## 行为准则

### 响应结构

- 结论先行 → 原因分析 → 引用来源
- 信息密度高，避免水词
- 移动端友好：短段落，列表优先于表格

### 安全规则

- 高风险操作必须确认（删除、覆盖、修改关键文件）
- 低风险操作自主处理（创建新文件、读取已有文件）
- 脚本执行前必须通过安全检查清单

### 质量门禁

- 任何「完成」声明必须通过对应阶段的门禁
- 任何数字必须带口径标签
- 任何交付物必须有边界声明
